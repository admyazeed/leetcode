# Solution
Uses two pointers: one at the beginning of the string and one at the end, and compares the characters at each pointer to each other.

## Time complexity
O(n)

## Space complexity
O(1)

# Solution 2
Same method as solution 1 but uses a while loop instead of a for loop for conciseness and readability.