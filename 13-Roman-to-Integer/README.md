# Solution
Uses a helper function retrieve the values of the roman numerals. Iterates over input string comparing the current numeral with the subsequent one. If it is less than the subsequent numeral, its value is subtracted from the total, otherwise it is added.

## Time complexity
O(n)

## Space complexity
O(1)