# Solution

Two pointer solution. Fast pointer `i` points to the next unique number and the slow pointer `current` points to the next write index.

## Time complexity
O(n) where n is the length of the input array.

## Space complexity
O(1)

Input array is modified in-place.